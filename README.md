
## 🛠️ วิธีการติดตั้ง

### 1. ติดตั้งเซิร์ฟเวอร์
- ใช้ XAMPP, WAMP, MAMP หรือ LAMP
- เปิด Apache และ MySQL

### 2. นำไฟล์ไปวางในโฟลเดอร์ `htdocs` (XAMPP) หรือเทียบเท่า
- ตั้งชื่อโฟลเดอร์โปรเจกต์ เช่น `gift-finder`

### 3. สร้างฐานข้อมูล
- เปิด phpMyAdmin
- สร้างฐานข้อมูลใหม่ชื่อ `gift_finder`
- นำไฟล์ `gift_finder.sql` มาอิมพอร์ต

### 4. ตั้งค่าเชื่อมต่อฐานข้อมูล
- แก้ไขไฟล์ `api/config.php`
- ใส่ข้อมูล username, password, host, database ให้ตรงกับเซิร์ฟเวอร์ของคุณ

### 5. เข้าถึงแอป
เปิดเบราว์เซอร์แล้วไปที่: http://localhost/

# 🎁 Gift Finder Web App — รายงานโครงงานเว็บไซต์ค้นหาของขวัญ

---

## 📁 โครงสร้างโฟลเดอร์และรายละเอียดไฟล์ทั้งหมด

### 🔹 รากโปรเจกต์ (`H3/`)

## 🌐 ฟีเจอร์หลัก

- ✅ สมัครสมาชิก / เข้าสู่ระบบ
- ✅ ค้นหาผลิตภัณฑ์ (Search Products)
- ✅ ดูเรื่องราวของผู้รับ (Stories & Recipients)
- ✅ จัดการรายการโปรด (Bookmarks)
- ✅ อัปโหลดเรื่องราวของตนเอง (Story Upload)
- ✅ ดูผลิตภัณฑ์ทั้งหมด (Show All Products)
- ✅ รองรับการแสดงผลบนมือถือ (Responsive Design)


---

## 🧩 รายละเอียดไฟล์แต่ละไฟล์

---

### 📂 โฟลเดอร์ `api/` — ระบบ API Backend (PHP)

| ไฟล์ | หน้าที่ |
|------|---------|
| `add_bookmark.php` | เพิ่มสินค้าลงในรายการโปรดของผู้ใช้ |
| `authenticate.php` | ตรวจสอบการเข้าสู่ระบบ (Login) |
| `check_bookmark.php` | ตรวจสอบว่าสินค้าถูกเพิ่มเป็นรายการโปรดแล้วหรือไม่ |
| `check_session.php` | ตรวจสอบสถานะการล็อกอินของผู้ใช้ |
| `config.php` | ไฟล์ตั้งค่าเชื่อมต่อฐานข้อมูล MySQL |
| `create_folder.php` | สร้างโฟลเดอร์สำหรับจัดเก็บเรื่องราวของผู้ใช้ |
| `delete_recipient.php` | ลบข้อมูลผู้รับของขวัญ |
| `get_all_bookmarks.php` | ดึงรายการโปรดทั้งหมดของผู้ใช้ |
| `get_all_product.php` | ดึงข้อมูลสินค้าทั้งหมดจากฐานข้อมูล |
| `get_bookmarks_by_product.php` | ดึงข้อมูลรายการโปรดตาม ID สินค้า |
| `get_folder_by_name.php` | ดึงโฟลเดอร์ตามชื่อ (ใช้กับเรื่องราว) |
| `get_product.php` | ดึงข้อมูลสินค้าเฉพาะรายการตาม ID |
| `get_products_in_folder.php` | ดึงสินค้าที่อยู่ในโฟลเดอร์ของเรื่องราว |
| `get_recipient.php` | ดึงข้อมูลผู้รับของขวัญ |
| `get_user_folders.php` | ดึงโฟลเดอร์ทั้งหมดของผู้ใช้ |
| `googlelogin.php` | ระบบล็อกอินด้วย Google (ยังไม่เสร็จสมบูรณ์) |
| `home.php` | หน้าโฮมของระบบ (อาจใช้แสดงข้อมูลหลังล็อกอิน) |
| `logout.php` | ออกจากระบบ (Destroy Session) |
| `register.php` | สมัครสมาชิกใหม่ |
| `remove_bookmark.php` | ลบสินค้าออกจากรายการโปรด |
| `save_recipient.php` | บันทึกข้อมูลผู้รับของขวัญ |
| `search_products.php` | ค้นหาสินค้าตามคำค้น |
| `stories_api.php` | API สำหรับดึงข้อมูลเรื่องราว (Stories) |
| `story_detail_api.php` | API ดึงรายละเอียดเรื่องราวเฉพาะเรื่อง |
| `test_json.php` | ไฟล์ทดสอบการแสดงผล JSON |
| `upload_story.php` | อัปโหลดเรื่องราว (รวมรูปภาพและข้อความ) |

---

### 📂 โฟลเดอร์ `assets/css/` — ไฟล์สไตล์ CSS

| ไฟล์ | หน้าที่ |
|------|---------|
| `form.css` | จัดรูปแบบฟอร์ม (เช่น ฟอร์มสมัคร, ฟอร์มค้นหา) |
| `global.css` | กำหนด Style พื้นฐานทั่วทั้งเว็บ (Font, Color, Margin, Padding) |
| `index.css` | จัดรูปแบบหน้าแรก (Home Page) |
| `login.css` | จัดรูปแบบหน้าเข้าสู่ระบบ |
| `mobile.css` | จัดรูปแบบสำหรับอุปกรณ์มือถือ (Responsive Design) |
| `product.css` | จัดรูปแบบหน้าแสดงรายละเอียดสินค้า |
| `stories.css` | จัดรูปแบบหน้าแสดงเรื่องราวของผู้รับ |
| `story-view.css` | จัดรูปแบบหน้าแสดงรายละเอียดเรื่องราว |
| `styles.css` | ไฟล์ CSS หลักที่รวม Style ทั่วไป (อาจรวมจากไฟล์อื่นๆ) |

---

### 📂 โฟลเดอร์ `assets/img/` — ไฟล์รูปภาพ

| ไฟล์ | หน้าที่ |
|------|---------|
| `demo-img-2.png` | รูปภาพตัวอย่างสำหรับแสดงบนหน้าเว็บ (เช่น แบนเนอร์, ไอคอน) |

---

### 📂 โฟลเดอร์ `assets/js/` — ไฟล์ JavaScript

| ไฟล์ | หน้าที่ |
|------|---------|
| `app.js` | ไฟล์หลักสำหรับควบคุมการทำงานทั่วไปของเว็บแอป |
| `data.js` | เก็บข้อมูลคงที่ (Static Data) หรือข้อมูลเริ่มต้น |
| `form.js` | จัดการเหตุการณ์ (Event) ของฟอร์ม เช่น การส่งข้อมูล, การตรวจสอบ |
| `header.js` | จัดการเมนูหัวเว็บ (Header), การแสดงสถานะผู้ใช้ |
| `product.js` | จัดการการทำงานของหน้าสินค้า (เช่น คลิกดูรายละเอียด, เพิ่มรายการโปรด) |
| `show_all_product.js` | จัดการการแสดงผลสินค้าทั้งหมด (เช่น การเรียงลำดับ, การค้นหา) |
| `stories.js` | จัดการหน้าเรื่องราว (Stories) เช่น การโหลดข้อมูล, การเลื่อนดู |
| `story-view.js` | จัดการหน้าแสดงรายละเอียดเรื่องราว (เช่น แสดงรูป, แสดงข้อความ) |

---

### 📄 ไฟล์ HTML หลัก

| ไฟล์ | หน้าที่ |
|------|---------|
| `about.html` | หน้าเกี่ยวกับเว็บไซต์ |
| `contact.html` | หน้าติดต่อเรา |
| `form.html` | หน้าฟอร์ม (อาจเป็นฟอร์มเพิ่มผู้รับหรือสินค้า) |
| `index.html` | หน้าแรกของเว็บไซต์ |
| `login.html` | หน้าเข้าสู่ระบบ |
| `my-stories.html` | หน้าแสดงเรื่องราวของผู้ใช้เอง |
| `product.php` | หน้าแสดงรายละเอียดสินค้า (ใช้ PHP ดึงข้อมูล) |
| `register.html` | หน้าสมัครสมาชิก |
| `show_all_product.html` | หน้าแสดงสินค้าทั้งหมด |
| `stories.html` | หน้าแสดงเรื่องราวของผู้รับ |
| `story-new.html` | หน้าเพิ่มเรื่องราวใหม่ |
| `story-view.html` | หน้าแสดงรายละเอียดเรื่องราว |

---

### 📄 ไฟล์อื่น ๆ

| ไฟล์ | หน้าที่ |
|------|---------|
| `.gitignore` | ระบุไฟล์/โฟลเดอร์ที่ Git ไม่ต้องติดตาม เช่น `node_modules`, `logs`, `config.php` |
| `gift_finder.sql` | ไฟล์ SQL สำหรับสร้างตารางฐานข้อมูล (Users, Products, Stories, Bookmarks, Recipients) |
| `README.md` | เอกสารคำอธิบายโปรเจกต์นี้ |

---

## ⚙️ วิธีการติดตั้งและรันโปรเจกต์

### 1. ติดตั้งเซิร์ฟเวอร์
- ใช้ XAMPP / WAMP / MAMP / LAMP
- เปิด Apache และ MySQL

### 2. วางโปรเจกต์
- วางโฟลเดอร์ `H3` ลงใน `htdocs` (XAMPP) หรือเทียบเท่า
- ตั้งชื่อโฟลเดอร์เป็น `gift-finder` (หรือชื่อใดก็ได้)

### 3. สร้างฐานข้อมูล
- เปิด phpMyAdmin → สร้างฐานข้อมูลชื่อ `gift_finder`
- อิมพอร์ตไฟล์ `gift_finder.sql`

### 4. ตั้งค่าฐานข้อมูล
- แก้ไขไฟล์ `api/config.php`
- ใส่ข้อมูล:
  ```php
  $host = "localhost";
  $username = "root"; // หรือชื่อผู้ใช้ของคุณ
  $password = "";     // รหัสผ่าน (ถ้ามี)
  $database = "gift_finder";

## 📝 หมายเหตุเพิ่มเติม

- โปรเจกต์นี้ยังอยู่ในระหว่างพัฒนา (Development Stage)
- ควรตรวจสอบความปลอดภัยของ API และการเชื่อมต่อฐานข้อมูลก่อนนำไปใช้งานจริง
- แนะนำให้ใช้ HTTPS เมื่อเปิดใช้งานจริง

## 🤝 ติดต่อ / สนับสนุน

หากคุณพบบั๊ก หรือมีข้อเสนอแนะในการปรับปรุง:

- สร้าง Issue บน GitHub: [Create Issue](https://github.com/yourusername/gift-finder/issues)
- หรือส่งอีเมลมาที่: your.email@example.com

---

✨ **ขอบคุณที่ใช้งาน Gift Finder!**  
ขอให้คุณค้นพบของขวัญที่สมบูรณ์แบบสำหรับคนพิเศษของคุณเสมอ 😊
